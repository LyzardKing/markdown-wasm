[build]
  command = "npm run setup && npm run build"
  publish = "dist"

# Required headers for SharedArrayBuffer (used by WASM runtimes)
[[headers]]
  for = "/*"
  [headers.values]
    Cross-Origin-Opener-Policy = "same-origin"
    Cross-Origin-Embedder-Policy = "require-corp"
